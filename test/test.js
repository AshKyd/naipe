var assert = require("assert");

var naipe = require('../src/');

describe('Deck', function() {
    describe('init', function() {
        it('should initialise', function() {
            var deck = new naipe.Deck();
        });
    });
});
